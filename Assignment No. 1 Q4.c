#include<stdio.h>
int main()
{
    float r;
    printf("Enter the radius of a circle : ");
    scanf("%f",&r);
    printf("Area of circle is %f having the radius %f.", 4*3.14*r*r,r);
    return 0;
}